#include<stdio.h>
int main()
{ int a,b,q,r;
  printf("enter dividend");
  scanf("%d",&a);
  printf("enter divisor");
  q=a/b;
  r=a-(b+q);
  printf("ther remainder is: %d",r);
  return 0;
  }